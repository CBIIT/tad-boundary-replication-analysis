# Helper scripts

These scripts are optional convenience wrappers. They do not replace the analysis notebooks.

- `check_inputs.R` - checks whether required files and raw data patterns are present.
- `render_all.R` - attempts to render the main notebooks, skipping analyses whose required raw data are missing.
